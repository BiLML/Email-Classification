---
trigger: always_on
---

Security:
- Never hardcode secrets, API keys, tokens, passwords, or credentials.
- Never expose sensitive data in logs, code comments, responses, or client-facing errors.
- Use environment variables for secrets.
- Use parameterized queries for database operations.
- Check security implications before working with auth, database, payment, user data, or internal business logic.
